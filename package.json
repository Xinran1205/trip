{
  "name": "sanya-trip",
  "version": "1.0.0",
  "private": true,
  "description": "Static Sanya travel notebook. Node is only used for local tooling.",
  "scripts": {
    "prepare:assets": "node scripts/prepare-assets.cjs",
    "test": "node --test tests/core.test.cjs",
    "test:browser": "playwright test",
    "preview": "node scripts/serve.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "coordtransform": "2.1.2",
    "lucide": "1.44.0",
    "sharp": "0.35.4"
  }
}
